/**
 * Arquivo de tokens gerado automaticamente
 * NÃO EDITE MANUALMENTE
 */

// Tokens importados diretamente dos arquivos originais
const colors = require('./src/design-system/tokens/colors');
const shadows = require('./src/design-system/tokens/shadows');
const spacing = require('./src/design-system/tokens/spacing');
const typography = require('./src/design-system/tokens/typography');
const unified_tokens = require('./src/design-system/tokens/unified-tokens');

module.exports = {
  "colors": colors,
  "shadows": shadows,
  "spacing": spacing,
  "typography": typography,
  "unified-tokens": unified_tokens,
};
